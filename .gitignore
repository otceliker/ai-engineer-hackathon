# Claude local settings (machine-specific, not for sharing)
.claude/settings.local.json

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.env
.env.*

# Model weights & datasets (large — never commit)
models/
data/
*.safetensors
*.gguf
*.bin

# Eval outputs (per-example JSONL with full generations — large)
results/

# Node
node_modules/
dist/

# OS
.DS_Store
